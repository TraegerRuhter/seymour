# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# playwright
/playwright-report/
/test-results/
/blob-report/
# The throwaway config for running the suite against this sandbox's older
# pre-installed browsers — see "Environment traps" in CLAUDE.md. Ignored rather
# than merely "remember to delete it", because forgetting silently pins CI to a
# browser path that only exists here.
/playwright.local.ts

# Benchmark corpus: ~20 MB, fetched on demand (npm run benchmark:fetch), never committed.
benchmark/data/
